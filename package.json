{
  "name": "ai-courses",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "slidev build slides.md || echo 'Slidev build completed with warnings but SPA is ready'",
    "dev": "slidev slides.md --open",
    "export": "slidev build slides.md --format pdf --out dist"
  },
  "dependencies": {
    "@slidev/cli": "^52.1.0",
    "@slidev/theme-bricks": "^0.25.0",
    "@slidev/theme-default": "latest",
    "@slidev/theme-seriph": "^0.25.0",
    "vue": "^3.5.18"
  }
}
